# Output

The products are ranked according to their price within each category.

## Observation

RANK() assigns rankings separately for every category.

## Time Complexity

O(n log n)

because SQL Server sorts records before ranking.
